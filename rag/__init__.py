"""Package RAG của dự án — cấu hình kotaemon, prompt, ingest, query.

Lưu ý: để import được package này, gốc repo phải nằm trên sys.path. Các entry
script (ingest_corpus, query_test, fast_ingest) và app/app.py đều tự chèn gốc
repo vào sys.path ở đầu file nên chạy trực tiếp được, không cần set PYTHONPATH.
"""
